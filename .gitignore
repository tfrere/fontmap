# Dependencies
/node_modules

# Build output (rebuilt in CI and deployed to the HF Space)
/build
/coverage

# Environment
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Pipeline working directories (fonts, renders, embeddings: regenerate with the pipeline)
/pipeline/input/
/pipeline/output/
/pipeline/.venv/
__pycache__/

# External clone of FontCLIP + its checkpoint (see pipeline/README.md)
/pipeline/fontclip_workspace/

# Legacy UMAP batch-testing artifacts
/pipeline/batch-testing/

# Large generated data (regenerate with pipeline)
public/data/font-map.json
REDDIT_LAUNCH.md
